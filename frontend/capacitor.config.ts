import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.opticlinic.patient',
  appName: 'OptiClinic Patient',
  webDir: 'dist/test'
};

export default config;
